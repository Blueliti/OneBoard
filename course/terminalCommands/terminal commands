cd = change directory 
to set the folder your working on on mac you can press cmd 
and drag the folder into terminal for it to put as the directorry
ls = list 
list what's inside the directory 
touch "name of file"."extension"= create a file
code. = open up the directory